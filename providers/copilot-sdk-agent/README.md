# Copilot SDK repository-agent provider

**PBI:** 010

Optional `RepositoryAgentProvider` implementation using GitHub Copilot SDK with BYOK,
including OpenAI-compatible endpoints, bounded turns/tools, normalized usage, and
capability declaration.

It receives a sanitized repository runtime and never owns checkout credentials.
