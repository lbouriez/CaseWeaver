# Provider template

**PBIs:** 003, 010

Reference layout and contract tests for a new embedding, vision, generation, reranking,
or repository-agent provider.

Provider-specific configuration is validated at the adapter boundary.
