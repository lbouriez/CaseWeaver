# AI configuration and cost

**PBI:** 003

Immutable provider/model bindings, model roles, LiteLLM catalog import, conditional price
overrides, usage normalization, cost calculation, and transactional budget reservation
policies.

Provider calls remain behind `ai-sdk`; persistence remains behind application ports.
