# Repository-change worker module

This composition-only module adapts the provider-neutral repository-change application feature to `ai-execution` and the existing immutable repository runtime resolver. It supplies model prompts and maps only bounded structured outputs. Azure DevOps, secret resolution, and persistence stay in their own outer adapters.
