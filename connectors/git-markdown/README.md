# Git/Markdown connector

**PBI:** 005

Knowledge source for local/remote Git repositories, Markdown and Docusaurus conventions,
blob-OID fingerprints, authentication, path filters, source URLs, and provenance.

Chunking and embedding belong to `packages/knowledge`.
